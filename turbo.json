{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["**/dist/**", "www/**", "hydrate/**", "docs/**", "src/components.d.ts"]
    },
    "build:watch": {
      "dependsOn": ["^build"],
      "outputs": ["**/dist/**", "www/**", "hydrate/**", "docs/**", "src/components.d.ts"],
      "cache": false,
      "persistent": true
    },
    "start": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    },
    "test": {},
    "clean": {
      "cache": false
    },
    "lint": {}
  }
}
