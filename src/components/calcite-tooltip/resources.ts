import Popper from "popper.js";

export const CSS = {
  container: "container",
  containerOpen: "container--open",
  containerFont: "font-size-3",
  imageContainer: "image-container",
  closeButton: "close-button"
};

export type TooltipPlacement = Popper.Placement;
