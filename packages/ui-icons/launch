#!/bin/bash

cd "$(dirname "$0")" || return
pnpm --dir ../.. install

# Prevent lint-staged errors. Turbo will cache the build, so this shouldn't impact startup times too much
pnpm exec turbo run build --filter @esri/eslint-plugin-calcite-components

pnpm start
